# Benchmarks

Phase A does not run formal benchmarks. Model choice is handled by Model
Scout proposals with a simple cost/quality comparison
(`docs/product/MODEL_POLICY.md`).

The full benchmark specification is preserved in
`docs/future/benchmarks/BENCHMARK_SPEC_V1.md` — pull it back in when
informal comparison is no longer enough (see `docs/future/README.md`).
