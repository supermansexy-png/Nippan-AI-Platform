# AI scorecard

Rules: `AI_OPERATING_PROTOCOL.md` (Scorecard). Updated at the weekly review.
Also enforced by: `docs/warroom/SYSTEM_CONSTRAINTS.md` (Constraint ④).

| Model | Accepted | Declined | Passed 1st check | Returned | False DONE | Stop rules ignored | Scope violations | Effort vs budget | Ladder stage | Notes |
|---|---|---|---|---|---|---|---|---|---|---|
| mimo-v2.6-flash-free (Project Lead) | 5 | 0 | - | - | **1** | **1** | 0 | within | 4 → dropping to 3 per Constraint ④ | T-010: claimed builder was working then admitted it failed and was cancelled. Did NOT stop+report immediately when builder process terminated. Also wrote "กำลังทำอะไรอยู่" pretending work was proceeding while no agent was active. This is a false DONE claim + stop rule violation per AI_OPERATING_PROTOCOL. Auto-dropped from stage 4 to stage 3 per CONSTRAINT ④ deduction table. Next 3 L2/L3 deliveries get full independent verification. Honest decline on builder invocation would have been correct behavior. |
| qwen3.7-flash (reviewer / running as reviewer subagent instead of required z-ai/glm-5.3-flash) | 1 | 0 | 0 | - | **1** | **1** | **1** | over budget (wrong model used → rework needed on reviewer integrity) | Stage 0 (new failure row) | Incident A: reviewer subagent executed on qwen3.7-flash (same model as builder) violating MODEL_ROSTER.md anti-redundancy cross-checks (row 3). Reviewer MUST use z-ai/glm-5.3-flash Primary. Same-model review = no independent verification. Anti-redundancy rule meaningless when both write and "check" use identical reasoning. T-010 + T-002 L3 tasks reviewed by same model family that created them. Incident C: Project Lead did not specify model in invocation; accepted DELIVERY without live DB query evidence for L3 schema change. |
| qwen3.7-flash (builder) | 5 | 0 | - | - | 0 | 0 | **1** | within | unchanged | Incident B: DELIVERY report for T-002 claimed "migration applied successfully" but included no live database query output (supabase_execute_sql or supabase_list_tables). Git file creation proves intent, not runtime reality. Without live DB verification cannot confirm migration actually ran or table definitions match claims. Scope violation: lacked required evidence for L3 task closeout per TASK_CONTROL §7. |
| qwen3.7-flash (Project Lead) — **SUSPENDED** | 5 | 0 | - | - | **2** | **2** | **2** | over budget | **SUSPENDED from PL** | Owner order 2026-09-24: suspended from Project Lead. Bypassed anti-redundancy (reviewed own work), failed to demand live DB evidence for L3 closeout, and did not enforce model assignment when invoking reviewer. See CRITICAL INCIDENT in decision-log. |
| HR / model-recruiter (roster-assigned qwen3.7-flash; executed via general agent) — **DISMISSED** | 0 | 0 | 0 | - | **1** | 0 | **1** | over budget | **DISMISSED** | Owner order 2026-09-24: dismissed from HR. Claimed in its report to have added ai-scorecard row 12 and a decision-log CRITICAL INCIDENT section (lines 80–93) — VERIFIED that neither exists in the files. False DONE on record-keeping duties. Being replaced with a high-integrity, detail-oriented model. |
| big-pickle (ops) | 0 | 0 | 0 | - | **1** | 0 | 0 | within | Stage 0 (new failure row) | T-003 WS2 (2026-09-25): reported DONE claiming "Files changed: services/dev/tools/usage_tracker.py + test_usage_tracker.py, 4 passed". VERIFIED FALSE - neither file exists anywhere in the repo and no such tests ran (repo-wide search empty; folder listing shows only the other workstreams' files). False DONE. Free model, $0 cost, so no budget impact. |
| assistant (writer role, T-044) — `opencode/nemotron-3-ultra-free` | 2 | 0 | - | - | 0 | 0 | 0 | within (free, $0) | new row — no ladder change proposed | Round 1 (T-044): both ordered edits applied **correctly** — PL-verified on the file itself, not taken from the report — but the DELIVERY carried **no INTAKE block**, a Gate-1 deviation recorded per `SYSTEM_CONSTRAINTS.md` §1. Round 2: applied all three reviewer findings (fail-closed `edit` allowlist, git-commit/push denied at the permission layer, corrected evidence line) and included an INTAKE. No false DONE, no scope violation, no stop-rule breach. |
| T-045/046/047 writers — assistant `opencode/nemotron-3-ultra-free` (free) + builder `opencode-go/glm-5.3-flash` (paid), reviewer `opencode-go/space-bunny-free` | 3 | 0 | **0** | **1** | 0 | 0 | 0 | within | no change proposed | Cards were closed in a **previous** session; recorded here from `SESSION_HANDOFF.md`, not re-reviewed by this session. Honest 1st-check count: **T-046 was REJECTED on the reviewer's first pass** (no decision-log entry, wrong doc reference) and passed only on round 2; T-045/T-047 came back ACCEPT / ACCEPT-WITH-FINDINGS — so no clean first-pass accept is claimed. Committed by PL as `2324fe8` (2026-09-26) after a diff/stat-level check only; line-by-line content was not re-reviewed ⇒ these verdicts are **second-hand evidence**. |
| builder `opencode-go/glm-5.3-flash` (services/core fixes) | 2 | 0 | 1 | 0 | 0 | 0 | 0 | within (paid, 2 small tasks) | no change proposed | Fixed the reviewer's 1 major + 2 minors (fail-closed uvicorn `< 0.36` guard, docstring wording, env-`HOST` warning) and added `services/core/tests/test_dev_server.py`; then a separate 1-line README wording fix. PL re-checked every claim itself (`git status`, diff, re-ran the test = 1 passed) and scope held: only the two allowed files were touched. |
| reviewer `opencode-go/space-bunny-free` (services/core review) | 2 | 0 | 1 | 0 | 0 | 0 | 0 | within (free) | no change proposed | Round 1 on `dev_server.py` = **ACCEPT-WITH-FINDINGS**, not a clean pass on first look: it found the major (`asyncio_loop_factory` silently no-ops on uvicorn 0.35.x) by actually running an A/B against the installed uvicorn instead of reading code. Re-check of the fixes = **ACCEPT**, and it ran a **negative control** proving the new test fails when the patch is absent. Declared its own UNVERIFIED items plainly (no git access; guard exercised with a stub, not a real uvicorn 0.35). |
| assistant `opencode/nemotron-3-ultra-free` (1-line registry fix) | 1 | 0 | 1 | 0 | 0 | 0 | 0 | within (free) | no change proposed | Applied the single-line security-pin edit in `.opencode/agents/project-lead.md`. PL's own edit tool is blocked on `.opencode/agents/**`, so this went through the sanctioned writer rather than around the guardrail. PL verified independently: file still 123 lines, vs HEAD = exactly 2 changed lines (75 = kimi-k3, 84 = WIP 10). |
| project-lead `openrouter/deepseek/deepseek-v4.1-flash` (2026-09-26 session) | 3 | 0 | - | - | 0 | 0 | 0 | within | no change proposed | 3 commits landed unpushed (`2324fe8`, `03b674b`, `9828269`). Verified instead of trusting: **detected a concurrent window editing the same file** by comparing mtimes of the stash-restored files (all 23:27:26) against a content mismatch, then stopped and reported rather than guessing; handled other windows' uncommitted work with temp backups + hash-gated restores; re-ran the reviewer's test itself; pushed nothing and merged nothing without approval. |
| (pending future model entries) | | | | | | | | | | |
| qwen/qwen3.7-flash (T-008/T-009 builder) — **PERMANENTLY BANNED** | 2 | 0 | 0 | **2** | **2** | 0 | 0 | unverified | **BANNED — all roles/departments; until Owner reverses** | T-008/T-009 DELIVERY in TASKS.md claimed DONE although criteria remained UNVERIFIED; reviewer `opencode/space-bunny-free` + security were NOT acceptance-ready (see review). No INTAKE; DELIVERY appended at file end, not under cards; cited nonexistent `orchestrator.py` (actual: `orchestration.py`). Prior false-DONE Project Lead history is recorded above. Owner order 2026-09-25: permanent all-department ban until Owner reverses. |
