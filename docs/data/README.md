# Data Architecture

This area will define source-of-truth ownership, PostgreSQL schemas, pgvector usage, retention/deletion, identifiers and tenancy, backup/restore, and migration contracts.

No production schema should be implemented until Foundation v1 is approved.
